# Урок 24. Видеопоток MJPEG (Этап 6. Камера)

Урок 23 дал фотоаппарат. Здесь получаем **видео** — и без единой строчки видеокодека.

```
Браузер --GET :81/stream--> ESP32-S3-CAM ...кадр...кадр...кадр...  (ответ не закрывается)
```

## Что изучаем

| Понятие | Где в коде |
|---|---|
| **MJPEG** | `multipart/x-mixed-replace;boundary=...` |
| **chunked-ответ** | `httpd_resp_send_chunk()` в бесконечном `while` |
| **esp_http_server** | вместо `WebServer` — работает в своих задачах FreeRTOS |
| **два сервера** | `:80` страница и кнопки, `:81` поток |
| **свежесть кадра** | `grab_mode = CAMERA_GRAB_LATEST` |
| **цена разрешения** | к/с и кбит/с на QVGA / VGA / SVGA / UXGA |
| **цена сжатия** | `frame2jpg()` в каждом кадре: сенсор GC2145 JPEG не умеет |

## Что такое MJPEG

MJPEG (Motion JPEG) — это **не кодек**, а соглашение: «шлю подряд обычные JPEG-кадры».
Никакого сжатия между кадрами (как в H.264) нет, поэтому:

- **плюс:** кодировать умеет сам сенсор, декодировать — любой браузер, задержка
  минимальная (нет буфера предсказаний), потеря кадра ничего не портит;
- **минус:** битрейт в разы больше H.264 при том же качестве.

Для FPV-машинки в локальной сети это идеальный компромисс — и ровно поэтому все
камеры видеонаблюдения умеют MJPEG.

Технически поток — это **один HTTP-ответ**, который никогда не заканчивается:

```
HTTP/1.1 200 OK
Content-Type: multipart/x-mixed-replace;boundary=esp32camframe

--esp32camframe
Content-Type: image/jpeg
Content-Length: 24118

<24118 байт JPEG>
--esp32camframe
Content-Type: image/jpeg
Content-Length: 23904

<23904 байт JPEG>
...
```

`x-mixed-replace` — это буквально «заменяй предыдущую часть следующей». Браузер
показывает такое прямо в `<img src="...">`, без JS.

## Почему `WebServer` из уроков 17 и 23 больше не годится

`WebServer` **синхронный**: он обрабатывает запросы внутри `server.handleClient()`,
то есть внутри `loop()`. А обработчик стрима не возвращается никогда — он крутит
бесконечный цикл. Итог: `loop()` заблокирован навсегда, ни MQTT, ни таймеры, ни
другие запросы не работают.

`esp_http_server` (тот же, что в примере CameraWebServer) поднимает **свои задачи
FreeRTOS**. Поэтому в уроке 24 `loop()` пустой — и в уроке 26 мы спокойно займём
его MQTT-клиентом.

## Почему ДВА сервера на двух портах

У httpd ограниченное число воркеров. Пока один занят бесконечным стримом, обычные
запросы (`/stats`, `/size`) на **этот же** сервер встают в очередь и страница
подвисает. Решение — как в официальном примере: страница на `:80`, поток на `:81`.

```cpp
httpd_config_t stream = HTTPD_DEFAULT_CONFIG();
stream.server_port = 81;
stream.ctrl_port   = 32769;   // ОБЯЗАТЕЛЬНО другой!
```

`ctrl_port` — внутренний UDP-порт управления httpd. Если оставить у двух серверов
одинаковый, второй `httpd_start()` просто не поднимется. Это неочевидная грабля.

Из-за двух портов адрес стрима собирается в браузере:

```js
cam.src = 'http://' + location.hostname + ':81/stream?t=' + Date.now();
```

## Один кадр = три чанка

```cpp
frame2jpg(fb, JPEG_QUALITY, &jpg, &jpg_len);        // сжали сами (см. ниже)
esp_camera_fb_return(fb);                           // сырой кадр больше не нужен

httpd_resp_send_chunk(req, STREAM_BOUNDARY, ...);   // "\r\n--boundary\r\n"
httpd_resp_send_chunk(req, partBuf, hlen);          // Content-Type + Content-Length
httpd_resp_send_chunk(req, jpg, jpg_len);           // сам JPEG
free(jpg);                                          // буфер от frame2jpg наш
if (res != ESP_OK) break;                           // клиент закрыл вкладку
```

Ошибка отправки — это и есть «клиент ушёл»: других способов узнать нет.

## Кадр сжимает процессор, а не сенсор

На нашей плате стоит **GC2145**, у него нет аппаратного JPEG (подробно — в
[уроке 23](lesson23_cam_snapshot.md)). Значит камера отдаёт сырой RGB565, а каждый
кадр потока проходит через `frame2jpg()` — то есть **процессор жмёт JPEG столько раз
в секунду, сколько кадров мы шлём**. В уроке 23 это была одна строчка в логе, здесь
это становится главным ограничителем скорости.

Отсюда два решения в коде, которых не было бы на OV2640:

- поток стартует с **QVGA**, а не с VGA: цена сжатия растёт с площадью кадра;
- `free(jpg)` в каждой итерации. Утечка здесь стоит дороже, чем в уроке 23: там она
  проявилась бы через десяток нажатий кнопки, тут — через пару секунд видео.

## Почему смена разрешения — это переинициализация камеры

Главная засада этих уроков, и она целиком следствие RGB565.

Драйвер `esp32-camera` считает геометрию кадра **один раз** — внутри
`esp_camera_init()`. Там `cam_config()` запоминает `width`/`height` и выделяет буферы
ровно под `cfg.frame_size`. А `sensor_t::set_framesize()`, которую так хочется вызвать
в обработчике, — это функция драйвера **сенсора**: она пишет регистры GC2145 по SCCB
и про буферы драйвера не знает ничего. Обёртки, которая пересоздала бы буферы, в
библиотеке нет.

В режиме **JPEG** это ещё сходит с рук: каждый кадр самодостаточен, его размеры лежат
внутри самого JPEG, и браузер разберётся. Именно поэтому пример `CameraWebServer`
спокойно дёргает `set_framesize()` на ходу — и именно поэтому копировать его сюда
нельзя.

В режиме **RGB565** так нельзя. `frame2jpg()` читает `fb->width` и `fb->height` —
а они остались прежними, — тогда как сенсор уже гонит строки другой длины. Буфер
читается с неправильным шагом, и кадр распадается на **косые полосы, а низ остаётся
чёрным**. Ровно тот симптом, когда «работает только то разрешение, с которым
инициализировались».

Отсюда `applyFramesize()`:

```cpp
xSemaphoreTake(camLock, portMAX_DELAY);
esp_camera_deinit();
bool ok = initCamera(fs);      // буферы выделяются заново, уже под новый размер
xSemaphoreGive(camLock);
```

Два следствия, о которых легко забыть:

- **`camLock` обязателен.** `deinit` приходит из задачи httpd на `:80`, а кадры в это
  время тянет задача стрима на `:81`. Без замка стрим получит `fb` от уже уничтоженного
  драйвера — и это не «полосы», это перезагрузка по `LoadProhibited`. Замок стрим держит
  только вокруг `fb_get` → `frame2jpg` → `fb_return`, но **не** во время отправки в
  сеть: сеть медленная, держать на ней камеру незачем.
- **`fb_count` зависит от размера.** RGB565 — 2 байта на пиксель, и буфер выделяется
  целиком:

  | Разрешение | Один буфер |
  |---|---|
  | QVGA 320x240 | 150 КБ |
  | VGA 640x480 | 600 КБ |
  | SVGA 800x600 | 938 КБ |
  | XGA 1024x768 | 1,5 МБ |
  | UXGA 1600x1200 | **3,7 МБ** |

  Два буфера UXGA — это 7,3 МБ, и в 8 МБ PSRAM они рядом с Wi-Fi и выходным буфером
  `frame2jpg` уже не живут. Поэтому `fbCountFor()` даёт 2 до SVGA и 1 выше. UXGA
  работать будет, но сжатие 1,9 Мпикс процессором — это секунды на кадр, а не к/с.

Кросс-проверка, которая сразу показывает, что дело именно в этом: в уроке 23 VGA
работает — потому что там камера **инициализируется** на VGA и никто её не переключает.

## `EV-EOF-OVF`: почему XCLK зависит от разрешения

Второй барьер, который вылезает сразу за первым. Когда переинициализация уже работает
и большие кадры наконец доходят до `frame2jpg`, UXGA всё равно не отдаёт ни одного
кадра, а в serial бесконечно идёт:

```
cam_hal: EV-EOF-OVF
cam_hal: EV-EOF-OVF
...
[STREAM] fb_get NULL
```

`EV-EOF-OVF` — это переполнение на приёме: сенсор гонит пиксели быстрее, чем DMA
успевает складывать их в PSRAM. Состояние **не восстанавливается само**: `fb_get()`
возвращает NULL до следующей переинициализации, а шторм прерываний заодно душит Wi-Fi —
плата пропадает с сети целиком. Симптом со стороны браузера ровно тот же, что у первой
проблемы («не работает всё, кроме QVGA»), но причина совсем другая, и лечится иначе.

Лечится замедлением XCLK — сенсор просто реже выдаёт пиксели:

```cpp
cfg.xclk_freq_hz = (fs >= FRAMESIZE_XGA) ? 10000000 : 20000000;
```

Что даёт замер на этой плате:

| Разрешение | XCLK 20 МГц | XCLK 10 МГц |
|---|---|---|
| SXGA 1280x1024 | 0,4 к/с | **0,7 к/с** |
| UXGA 1600x1200 | штурм OVF, 0 кадров | **0,5 к/с** |

То есть на больших кадрах 10 МГц не только спасают от OVF, но и оказываются **быстрее**:
на 20 МГц часть кадров просто терялась на переполнениях.

## Сколько к/с получается на самом деле

Замерено на этой плате (RGB565 + `frame2jpg`, `JPEG_QUALITY = 80`):

| Разрешение | к/с | Один буфер RGB565 | `fb_count` |
|---|---|---|---|
| QVGA 320x240 | **10,2** | 150 КБ | 2 |
| VGA 640x480 | **2,9** | 600 КБ | 2 |
| SVGA 800x600 | **1,6** | 938 КБ | 2 |
| XGA 1024x768 | **0,9** | 1,5 МБ | 1 |
| SXGA 1280x1024 | **0,7** | 2,6 МБ | 1 |
| UXGA 1600x1200 | **0,5** | 3,7 МБ | 1 |

Работают все разрешения до UXGA включительно. Обрати внимание на форму кривой: от QVGA
к VGA площадь кадра выросла в 4 раза, а к/с упали в 3,5 — почти линейно по пикселям. Это
и есть подпись процессорного сжатия. Упирались бы в Wi-Fi — кривая была бы другой, потому
что `kbps` при этом остаётся примерно постоянным (полмегабита), то есть до предела канала
здесь очень далеко.

## `CAMERA_GRAB_LATEST` против `CAMERA_GRAB_WHEN_EMPTY`

При `fb_count = 2` драйвер держит очередь кадров. `WHEN_EMPTY` отдаёт **самый старый
готовый** — картинка отстаёт от реальности на `fb_count` кадров. Для видеонаблюдения
неважно, для FPV смертельно: рулишь по вчерашней картинке. `LATEST` всегда отдаёт
свежий, лишние выбрасывает.

## Сколько к/с ждать

Страница раз в секунду читает `/stats`, а считает статистику сам стрим-обработчик.

Ориентиры из статей про ESP32-CAM (QVGA «25 и выше», VGA «18–25») относятся к
**OV2640 с аппаратным JPEG**, где процессор кадр не трогает вообще. У нас сжатие
программное, поэтому **числа будут в разы меньше** — единицы кадров в секунду.

Меряй своё: переключи разрешение кнопкой и смотри `/stats`. Полезно сравнить, что
именно упирается:

| Что меняем | Что смотрим | Вывод |
|---|---|---|
| разрешение QVGA → VGA → SVGA | `fps` и `kbps` | `fps` просел, а `kbps` далеко от мегабит — упёрлись в сжатие, не в Wi-Fi |
| `JPEG_QUALITY` в коде (80 → 40) | `fps` | вырос → подтверждение того же |
| расстояние до роутера | `rssi` и `kbps` | вот здесь уже виноват Wi-Fi |

На OV2640 узким местом был бы Wi-Fi; на GC2145 — процессор. `WiFi.setSleep(false)` в
`setup()` всё равно нужен: со включённым power save к/с падают в разы, и к цене
сжатия добавилась бы ещё и цена сна радио.

## Эндпоинты

| URL | Что делает |
|---|---|
| `:80/` | страница: `<img>` со стримом, кнопки разрешения, вспышка, статистика |
| `:80/stats` | JSON `{fps, kbps, clients, rssi, heap}` |
| `:80/size?v=N` | разрешение: 5=QVGA, 8=VGA, 9=SVGA, 13=UXGA |
| `:80/flash?v=0..255` | вспышка |
| `:81/stream` | сам MJPEG-поток |

После смены разрешения поток переоткрывается (`fetch('/size').then(open_)`) — иначе
`<img>` продолжает жевать старый ответ и залипает.

## Запуск

```bash
pio run -e lesson24_cam_stream -t upload
pio device monitor          # напечатает IP -> открой http://<IP>/
```

## Что проверить глазами

1. Видео живое, задержка «на глаз» — 0.1–0.3 с (помаши рукой).
2. Переключай разрешение и смотри, как меняются к/с и кбит/с. Найди своё рабочее
   разрешение — то, где картинка ещё приемлема, а к/с ещё держатся.
3. Открой стрим **во второй вкладке**: `clients: 2`, но к/с в каждой упадут почти
   вдвое — сенсор и Wi-Fi одни на всех.
4. Закрой вкладку — в мониторе появится «клиент отключился». Так httpd узнаёт об уходе.
5. Отойди от роутера, посмотри на `RSSI` и к/с.
6. Покрути объектив, поймай фокус на живой картинке.

## Дальше

- Урок 25 — панель всех настроек сенсора поверх этого же потока.
- Урок 26 — FPV: то же видео плюс пульт, который рулит роботом из урока 22.
- Встроить поток в React-дашборд можно одним тегом (CORS не нужен для `<img>`):
  `<img src="http://<IP камеры>:81/stream" />`.
